<input
    type="checkbox"
    class="orden-select h-4 w-4 rounded border-zinc-600 bg-zinc-800 text-purple-600 focus:ring-purple-500"
    data-id="{{ $orden->id }}"
    aria-label="Seleccionar orden {{ $orden->folio }}"
>